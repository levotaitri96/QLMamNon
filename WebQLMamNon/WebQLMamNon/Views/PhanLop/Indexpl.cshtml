@model IEnumerable<WebQLMamNon.Models.Tbl_PhanCong>

@{
    ViewBag.Title = "Indexpl";
    Layout = "~/Views/LayoutAdmin.cshtml";
}
@if (TempData["loipc"] != null)
{
    <script>
        alert("Mỗi giáo viên chỉ được dạy 1 lớp");
    </script>
}
@if (TempData["loi"] != null)
{
    <script>
        alert("Mỗi lớp tối đa 2 giáo viên ");
    </script>
}
<h2>Indexpl</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>

@using (Html.BeginForm("PhanCong", "PhanLop",FormMethod.Get))
{
<div class="form-group" style="float:left;width:90%">
    @Html.DropDownList("maLop", null, htmlAttributes: new { @class = "form-control", @style = "width:20%; float: left ;margin-right: 10px",@value="maLop"})
    @Html.DropDownList("maGV", null, htmlAttributes: new { @class = "form-control", @style = "width:25%; margin-right: 10px",@value="maGV" })
</div>

<input style="float:left" type="submit" value="Phan cong" />
}
    <table class="table">
        <tr>
            <th>
                ma lop
            </th>
            <th>
                ten lop
            </th>
            <th>
                hoten
            </th>
            <th></th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.trangThai)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Tbl_GiaoVien.trinhDo)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Tbl_LoaiLop.tenLoai)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Tbl_LopHoc.tenLop)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Tbl_GiaoVien.hoTen)
                </td>
                <td></td>
                <td>
                    @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) 
                </td>
            </tr>
        }

    </table>
